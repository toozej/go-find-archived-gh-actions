---
services:
  go-find-archived-gh-actions:
    container_name: go-find-archived-gh-actions
    image: toozej/go-find-archived-gh-actions:latest
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
    read_only: true
    labels:
      - "traefik.enable=false"
      # labels below for publishing as web service
      # - "traefik.docker.network=traefik"
      # - "traefik.http.routers.go-find-archived-gh-actions.rule=Host(`go-find-archived-gh-actions.test`)"
      # - "traefik.http.routers.go-find-archived-gh-actions.middlewares=http-redirect-https@file,headers@file"
      # - "traefik.http.routers.go-find-archived-gh-actions-secure.entrypoints=https"
      # - "traefik.http.routers.go-find-archived-gh-actions-secure.rule=Host(`go-find-archived-gh-actions.test`)"
      # - "traefik.http.routers.go-find-archived-gh-actions-secure.middlewares=headers@file"
      # - "traefik.http.routers.go-find-archived-gh-actions-secure.tls=true"
    # networks:
      # - traefik
